interface:
  display_name: "Video Semantic Graph Builder"
  short_description: "Build timestamp-aware video search graphs"
  default_prompt: "Use $build-video-semantic-graph to turn entry and result video folders into a validated timestamp-aware search graph."
